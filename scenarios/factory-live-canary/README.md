# Factory live canary

Run:

```bash
node scenarios/factory-live-canary/status.mjs "In Progress"
```

Test:

```bash
node --test scenarios/factory-live-canary/status.test.mjs
```

Normalized labels are only canonical text values. They confer no execution,
verification, publication, merge, or deployment authority.
