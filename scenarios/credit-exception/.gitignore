# Byte-compiled / cache
__pycache__/
*.py[cod]

# Local virtual environments (never required by this scenario)
.venv/
venv/

# Evidence images and logs are regenerated by the verification commands.
# They are committed deliberately (see EVIDENCE.md), so nothing here is
# ignored under evidence/ except scratch files.
evidence/*.tmp
evidence/scratch/

# Editor noise
.DS_Store
Thumbs.db
*.swp
